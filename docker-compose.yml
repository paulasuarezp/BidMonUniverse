version: '3.5'

services:
  restapi:
    build: ./restapi
    ports:
      - "5001:5001"
    env_file:
      - .env

  webapp:
    build: ./webapp
    ports:
      - "3000:3000"
    depends_on: 
      - restapi