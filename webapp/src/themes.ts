// src/themes.ts
import { createTheme } from '@mui/material/styles';

export const lightTheme = createTheme({
  palette: {
    mode: 'light',
    // Personalizar más colores aquí
  },
  // Añadir más personalizaciones aquí
});

export const darkTheme = createTheme({
  palette: {
    mode: 'dark',
    // Personalizar más colores aquí
  },
  // Añadir más personalizaciones aquí
});
